import SharedLayout from "./SharedLayout";
import CartLoader from "./CartLoader";

const CartView = () => {
  return (
    <SharedLayout>
      <CartLoader />
    </SharedLayout>
  );
};

export default CartView;
